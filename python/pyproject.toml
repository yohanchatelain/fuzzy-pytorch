[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fuzzy-torch"
version = "0.1.0"
description = "Set PRISM's virtual precision and rounding mode from Python"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["fuzzy_torch"]
